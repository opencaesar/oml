apply plugin: 'io.opencaesar.ecore2bikeshed'

ecore2bikeshed {
	inputPath = 'io.opencaesar.oml/src/io/opencaesar/oml'
	outputPath = 'io.opencaesar.oml.specification'
}

clean {
	delete 'src-gen'
	delete 'image-gen'
}
